# 개선 작업 TODO

> **Status (2026-05-13):** 1단계(기획) 완료, Phase 0 머지 완료. 이 파일의 도구별 항목은 **Phase 1 마이그레이션 시 해당 도구 작업의 ticket source 로 점진 흡수**됨 — 일괄 분해 안 함. 우선순위·전체 로드맵은 `docs/PRD_Z.md` §8.
>
> 진행 순서: 기획 확정 → 기능 완성 → 리디자인 → 문서화

---

## 1단계: 기획 (선행 필수)

- [ ] 서비스 이름 / 컨셉 / 타겟 정하기
  - Notes: 리디자인, README, 메뉴 구조 등 이후 모든 작업의 방향이 결정됨. 먼저 확정 필요.

---

## 2단계: 기능 개선

- [ ] 공통 — 작업 완료 후 재실행 버튼 추가
  - Acceptance: 파일 처리 완료(done) 상태에서 "다시 작업하기" 버튼 클릭 시 초기 상태로 리셋, 동일 파일 재업로드 없이 바로 재처리 가능
  - Notes: `ProcessingStatus` 컴포넌트 또는 각 도구 페이지의 상태 리셋 로직에서 처리. 모든 도구에 일괄 반영 가능.

- [ ] `/ppt-background` — 문구 수정 (.ppt 미지원 안내 표현 개선)
  - Acceptance: "PPT 파일" 표현을 "PPTX 파일"로 교체, .ppt는 별도 안내 문구 유지
  - Notes: .ppt 업로드 시 이미 변환 안내 UI가 있음 — 해당 UI 내 표현도 함께 검토.

- [ ] `/image-resize` — 픽셀 지정 모드에 해상도 프리셋 추가
  - Acceptance: 픽셀 입력 필드 하단에 FHD(1920×1080), HD(1280×720), 모바일(390×844) 등 프리셋 버튼 표시. 선택 시 너비·높이 자동 입력.
  - Notes: 기존 '프리셋' 탭의 프리셋 항목들과 중복 검토 필요.

- [ ] `/image-resize` — 기존 프리셋 탭을 비율(종횡비) 선택으로 개편
  - Acceptance: 기존 픽셀 단위 프리셋(1920×1080 등) 대신 정방형(1:1), 세로(9:16), 세로(3:4), 가로(16:9), 가로(4:3) 등 비율 선택 버튼 제공. 선택 시 크롭 UI 연동.
  - Notes: 기존 크롭 UI(`CropSelector.tsx`) 재활용 가능.

- [ ] `/ppt-background` — 슬라이드 지정 적용 기능 추가
  - Acceptance: "전체 슬라이드" 외에 "슬라이드 지정" 모드 추가. 슬라이드 번호 입력(예: `1, 3, 5-7`) 또는 썸네일 선택으로 적용 대상 지정 가능.
  - Notes: 현재 전체·마스터 2가지 모드만 존재. 슬라이드 썸네일은 `extractCurrentBackgrounds.ts` 로직 활용 가능.

- [ ] PPTX 파일 압축 기능 추가
  - Acceptance: PPTX 업로드 후 내부 이미지 품질 조정(압축 레벨 선택) 방식으로 파일 크기 감소. 압축 전·후 크기 비교 표시 후 다운로드.
  - Notes: PPTX는 ZIP 구조이므로 `jszip`으로 언패킹 후 이미지 리컴프레스(Canvas API or `browser-image-compression`) → 재패키징. 신규 도구(`/tools/ppt-compress`)로 추가하거나 기존 PPT 관련 도구에 통합.

- [ ] 파일 최대 용량 상한 검토 및 조정
  - Acceptance: 비로그인 기준 현재 10MB 상한을 조정(예: 20~30MB). 변경 시 `src/lib/constants.ts`의 `FILE_SIZE_LIMIT` 수정 및 UI 안내 문구 일괄 반영.
  - Notes: PPTX 압축 기능 구현 이후 검토하면 상한 조정 근거 확보됨. 서버리스 환경의 메모리 제약 함께 확인.

---

## 3단계: 리디자인

- [ ] 상단 메뉴(헤더 내비게이션) 정리
  - Acceptance: 1단계에서 확정된 컨셉·타겟에 맞게 메뉴 항목 재구성. 도구 카테고리 분류 및 접근 구조 개선.
  - Notes: `src/components/layout/Header.tsx` 수정. 메뉴 구조 확정 후 리디자인 작업과 병행 가능.

- [ ] 웹사이트 전체 리디자인
  - Acceptance: 확정된 서비스 컨셉·타겟에 맞는 새 디자인 시스템 적용. 메인 페이지, 도구 페이지, 공통 컴포넌트 전체 반영.
  - Notes: 리디자인 전 Figma 등 목업 작업 권장. Shadcn/UI 테마 커스터마이징 또는 교체 검토.

---

## 4단계: 문서화

- [ ] README.md 재작성
  - Acceptance: 확정된 서비스명·기능 목록·기술 스택 기반으로 전면 재작성. 로컬 실행 방법, 도구 목록, 기여 가이드 포함.
  - Notes: 리디자인과 기능 확정이 모두 완료된 후 작성해야 재작업 없음.
